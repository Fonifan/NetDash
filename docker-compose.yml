version: "3"

services:
  app-db:
    image: postgres:latest
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: app-db
    ports:
        - 5962:5432
  client-db:
    image: postgres:latest

    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: user
      POSTGRES_DB: user-db
    depends_on:
      - app-db
    ports:
      - 5961:5432
